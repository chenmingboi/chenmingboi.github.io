# 个人学术主页维护手册

## 1. 内容入口

日常只修改 `src/data/profile.js`，所有信息必须真实且由本人确认。

| 字段 | 用途 |
| --- | --- |
| `site.url` | 正式 HTTPS 根网址，末尾保留斜杠；同步生成 canonical、Open Graph、JSON-LD、robots 和 sitemap |
| `site.title / description` | 搜索摘要和分享文字，与可见页面事实一致 |
| `site.updated` | 内容实际更新时间，格式 YYYY-MM-DD；用于页脚、JSON-LD 和 sitemap |
| `identity.name / initials / subtitle` | 姓名、装饰性缩写、当前单位 |
| `identity.role` | 可选职务或身份，没有确认信息就省略 |
| `about` | 自我介绍段落数组，不写空字符串 |
| `researchInterests` | 已确认的研究方向数组 |
| `links` | 邮箱、学术主页、CV 等已确认链接，自动出现在简介和联系区 |
| `publications` | 论文，按年份从新到旧显示 |
| `projects` | 项目，保持作者填写的顺序，空数组隐藏栏目 |
| `awards` | 奖项，按年份从新到旧显示，空数组隐藏栏目 |

现有 NJU 缩写沿用原始资料。具体院系、职务、教育与研究经历尚未提供，不能据此推断。当前研究兴趣仅采用站主给出的 Computer systems 和 LLM systems。

## 2. 链接

`links` 是对象数组，每个对象包含 `label` 与 `href`。可用 HTTPS、mailto 或站点内绝对路径；不使用 `#`、空链接或未经确认的账号。

- 公开个人学术账号可额外设置 `kind: 'profile'`，它才会进入 Person 的 `sameAs`。
- CV / 论文 PDF 等资源不添加 `kind`。
- 文件放在 `public/`，例如 `public/cv.pdf` 对应 `/cv.pdf`。构建会检查本地文件存在。
- 邮箱保留 `mailto:` 前缀；页面同时显示可复制地址。
- 所有资源保持浏览器原生导航，不强制新开窗口。

## 3. 论文数据

每篇包含以下字段；此处仅说明格式，不提供可误发布为个人经历的示例条目。

| 字段 | 要求 |
| --- | --- |
| `id` | 稳定、非空、集合内唯一的字符串 |
| `year` | 四位年份字符串 |
| `title` | 正式完整标题 |
| `authors` | 按正式署名顺序排列的 `{ name, self? }` 数组；本人加 `self: true` |
| `venue` | 正式会议/期刊，预印本应明确写明 |
| `status` | 可选发表状态或报告形式，不能把投稿中写为接收 |
| `highlight` | 可选且已确认的荣誉 |
| `summary` | 可选的一句话研究问题或贡献；不添加未经证实的性能数字 |
| `links` | 可选资源数组，label 可为 Paper、Code、Project、Slides 等 |
| `bibtex` | 可选原始 BibTeX 字符串，通过原生折叠组件展示 |

年份自动排序，同年条目保留输入顺序；作者顺序不会排序。无 BibTeX 或资源时不显示控件。

## 4. 项目与奖项

项目：`{ id, name, summary, role?, stack?, links? }`。说明实际问题、自己的贡献以及可公开复现的资源；`stack` 为字符串数组，不必为了视觉效果填写技术标签。

奖项：`{ id, year, title, issuer?, description?, href? }`。只有确实取得的奖项才加入。

需要教育、研究经历或其他栏目时，先准备准确的时间、机构、身份与公开依据，再扩充数据与组件。现阶段不显示空履历区。

## 5. 页面与样式

- `src/App.jsx` 负责顺序：简介 / 论文 / 可选项目 / 可选奖项 / 联系方式。
- `getNavigation(data)` 位于资料文件，与可选栏目保持同步。
- `src/index.css` 控制字级、颜色、间距、断点、焦点、减少动态效果和打印样式。
- 桌面为双栏，760px 及以下为单栏；手机导航持续可见且可换行。
- 首个 Tab 聚焦“Skip to main content”；Enter 后焦点进入主内容。
- 当前使用系统默认指针，旧 cursor SVG 仍保留但不加载。
- `public/favicon.svg` 是轻量 MC 图标；真实头像与分享图片尚未提供。

## 6. SEO 与静态 HTML

`index.html` 是模板，不手工重复填写个人事实。Vite 从 profile 注入标题和元数据；`scripts/prerender.mjs` 从 React 组件生成完整正文，写入 `dist/index.html`。

生产 `dist/robots.txt` 和 `dist/sitemap.xml` 每次构建重新生成。public 中原有同名文件仍保留，发布使用构建结果。

网站只有一个实际页面，锚点无需分别加入 sitemap。正式资料新增后，重新构建即可同时更新可见正文与抓取内容。

## 7. 本地验证与发布

```bash
npm ci
npm run lint
npm test
npm run build
npm run preview -- --host 127.0.0.1 --port 4173
```

浏览器检查：

1. 1440px 桌面、768px 平板、390px 和 320px 手机无横向滚动。
2. 每个导航都能抵达存在的章节，返回顶部有效。
3. Tab 焦点可见，正文跳转有效，BibTeX 可用 Enter 展开。
4. 邮箱与所有真实外链目的地址正确。
5. 新条目没有泄漏内部备注、占位符或测试数据。

已构建后可运行 `npm run preview:fixtures`，打开 README 中的两个本机 QA 地址，检查完整静态页面和长文献条目。测试夹具使用 Test Researcher，与真实主页完全隔离。

```bash
git add src/data/profile.js
git commit -m "docs: update confirmed academic profile"
git push origin main
gh run list --workflow deploy.yml --limit 1
```

如果改过其他源码，提交时显式添加对应文件。检查失败时先修复首个报错，尤其是资料字段、链接和 Node 版本；不要跳过检查发布。

## 8. 尚待本人提供

- 更完整的简介、院系与当前身份、研究问题。
- 真实论文及作者顺序、发表状态、Paper / Code / BibTeX。
- 真实项目、本人贡献与可复现说明。
- 真实教育/研究经历、奖项。
- CV、Google Scholar / ORCID / GitHub 的确认地址。
- 本人照片或可公开使用的分享图。

搜索引擎的收录与排名不由构建结果保证。可在本人管理的 Search Console 中提交 sitemap 并观察抓取结果。
