# MingboChen 个人主页使用手册

这份手册用于说明：要修改页面上每个模块的内容，应编辑哪个文件、哪个字段。日常更新个人资料时，优先只修改 `src/data/profile.js`，不需要改 React 组件或样式文件。

## 1. 页面模块与文件对应关系

| 页面位置 | 内容文件与字段 | 负责展示的组件 | 样式位置 |
| --- | --- | --- | --- |
| 顶部导航 | `src/data/profile.js` → `navigation` | `src/components/Navbar.jsx` | `src/index.css` 中的 `.topbar*`、`.desktop-nav`、`.mobile-nav` |
| 左侧个人信息 | `src/data/profile.js` → `identity`、`links.email` | `src/components/Hero.jsx` | `src/index.css` 中的 `.profile-*` |
| 自我介绍 | `src/data/profile.js` → `about` | `src/components/About.jsx` | `src/index.css` 中的 `.prose-copy`、`.content-empty` |
| 发表论文 | `src/data/profile.js` → `publications` | `src/components/Publications.jsx` | `src/index.css` 中的 `.publication-*` |
| 所获奖项 | `src/data/profile.js` → `awards` | `src/components/Awards.jsx` | `src/index.css` 中的 `.award-*` |
| 页脚 | 姓名取自 `src/data/profile.js` → `identity.name` | `src/components/Footer.jsx` | `src/index.css` 中的 `.site-footer` |
| 黑色鼠标指针 | `public/cursor.svg`、`public/cursor-text.svg`、`public/cursor-link.svg` | 无 | `src/index.css` 顶部的三组 `cursor` 规则 |
| 搜索结果标题与描述 | `index.html` 中的 `<title>`、description、canonical、Open Graph 和 JSON-LD | 无 | 无 |
| 搜索引擎抓取入口 | `public/robots.txt`、`public/sitemap.xml` | 无 | 无 |
| 模块排列顺序 | 无 | `src/App.jsx` 中 `<main>` 内的组件顺序 | 无 |

## 2. 修改左侧个人信息

编辑 `src/data/profile.js` 中的 `identity` 和 `links`：

```js
identity: {
  name: '页面显示的姓名',
  initials: '头像中的缩写',
  subtitle: '学校、院系或当前单位',
},
links: {
  email: 'mailto:你的邮箱地址',
},
```

- `name` 同时用于左侧姓名、顶部站点名和页脚版权姓名。
- `initials` 建议填写 2–3 个字母。
- `subtitle` 目前显示为 `NJU`。
- `email` 必须保留 `mailto:` 前缀，点击后才能唤起邮件客户端。
- 如果要新增 GitHub、Google Scholar、ORCID 等入口，需要同时修改 `src/data/profile.js` 的 `links` 和 `src/components/Hero.jsx` 中的链接列表。

## 3. 修改自我介绍

编辑 `src/data/profile.js` 中的 `about` 数组。一项代表一个段落：

```js
about: [
  '第一段自我介绍。',
  '第二段可以介绍研究方向、教育经历或近期关注的问题。',
],
```

当前数组内只有空字符串，因此页面只显示“信息确认后补充”的状态，不会展示未确认的个人介绍。填写真实内容后，空状态会自动消失。

如需修改模块标题 `About`，编辑 `src/components/About.jsx` 中的 `<h2>` 文本。

## 4. 添加或修改论文

论文数据位于 `src/data/profile.js` 的 `publications` 数组。每篇论文使用以下结构：

```js
publications: [
  {
    year: '2026',
    title: '论文完整标题',
    authors: [
      { name: 'Mingbo Chen', self: true },
      { name: '合作者姓名' },
    ],
    venue: '会议或期刊全称（常用缩写），2026',
    status: 'Oral',
    highlight: 'Best Paper',
    links: [
      { label: 'Paper', href: 'https://论文地址' },
      { label: 'Code', href: 'https://代码仓库地址' },
      { label: 'Project', href: 'https://项目主页地址' },
      { label: 'BibTeX', href: 'https://BibTeX地址' },
    ],
  },
],
```

字段说明：

- `year`：用于按年份分组。建议按新到旧排列数组，页面会保持填写顺序。
- `title`：论文正式标题，应与论文首页一致。
- `authors`：作者顺序应与论文一致；在本人姓名上添加 `self: true`，页面会加粗并加下划线。
- `venue`：建议写“会议/期刊全称（缩写），年份”，例如 `Conference Name (CONF), 2026`。
- `status`：可选，适合填写 `Oral`、`Spotlight`、`Poster`、`Accepted` 或 `Preprint`；不需要时删除该行。
- `highlight`：可选，适合填写 `Best Paper`、`Honorable Mention` 等荣誉；没有则删除该行。
- `links`：可按实际情况保留 `Paper`、`Code`、`Project`、`BibTeX`、`Slides`、`Poster` 等链接；没有的入口直接删除。
- 不要使用 `href: '#'` 作为占位链接。未确认链接时，直接不写该链接项。

添加第二篇论文时，在第一篇对象后继续添加一个 `{ ... }`。相同 `year` 的论文会自动归到同一年份下。`publications: []` 为空时不会生成虚构论文。

如需调整论文的布局或颜色，修改 `src/index.css` 中所有以 `.publication-` 开头的规则；如需修改字段的展示顺序，编辑 `src/components/Publications.jsx`。

## 5. 添加或修改奖项

奖项数据位于 `src/data/profile.js` 的 `awards` 数组：

```js
awards: [
  {
    year: '2026',
    title: '奖项正式名称',
    issuer: '授予机构或赛事名称',
    description: '可选的一句话说明',
    href: 'https://官方证明或详情地址',
  },
],
```

- `year` 和 `title` 是建议保留的核心字段。
- `issuer`、`description`、`href` 都是可选字段，没有真实内容时可直接删除。
- 建议按年份从新到旧排列。
- 不要添加未获确认的奖项，也不要用 `#` 作为占位链接。
- `awards: []` 为空时不会生成虚构奖项。

如需调整奖项布局，修改 `src/index.css` 中所有以 `.award-` 开头的规则；如需修改字段展示顺序，编辑 `src/components/Awards.jsx`。

## 6. 修改导航、栏目名称与顺序

导航文字和锚点位于 `src/data/profile.js`：

```js
export const navigation = [
  { id: 'home', label: 'Home' },
  { id: 'about', label: 'About' },
  { id: 'publications', label: 'Publications' },
  { id: 'awards', label: 'Awards' },
]
```

- `label` 是导航中显示的文字，可以改为中文。
- `id` 必须和对应组件 `<section id="...">` 中的值完全一致，否则点击导航后无法定位。
- 右侧三个核心模块的排列顺序在 `src/App.jsx` 的 `<main>` 中控制。调整 `<About />`、`<Publications />`、`<Awards />` 的先后顺序即可改变页面顺序。
- 页面目前必须保留自我介绍、发表论文、所获奖项三个模块。

## 7. 修改黑色鼠标指针

- 普通区域的小箭头：`public/cursor.svg`，尺寸为 14×18，使用黑色主体、0.65px 窄白边和圆角曲线轮廓。
- 正文悬浮和选择文字时的衬线式 I-beam：`public/cursor-text.svg`，尺寸为 10×19，使用黑色内芯、白色描边、弧形端帽和中央短横。
- 链接和按钮上的点击手势：`public/cursor-link.svg`，尺寸为 18×22。
- 三种状态的元素分配和点击热点都位于 `src/index.css` 顶部的 `cursor` 规则中。
- 箭头规则中的 `2 1`、文字规则中的 `5 10`、链接规则中的 `6 3` 是各自的点击热点坐标。如果修改图形尺寸，需要同步调整对应数字。
- 如果希望恢复系统默认指针，删除这三组 `cursor` 声明即可。
- 文字选中颜色由 `src/index.css` 的 `::selection` 和 `::-moz-selection` 控制，当前使用 Apple 系统蓝和白色文字。
- 触摸屏设备不会显示鼠标指针，这是正常行为。

## 8. 修改搜索引擎信息

- 搜索结果标题：修改 `index.html` 中的 `<title>`，建议始终保留完整姓名。
- 搜索结果摘要：修改 `index.html` 中 `name="description"` 的内容；Open Graph 和 Twitter description 应同步更新。
- 姓名与别名：修改 `index.html` 中 JSON-LD 的 `name` 和 `alternateName`。只填写本人确认过的中文名、英文名或拼音形式。
- 正式网址：`index.html` 的 canonical、Open Graph URL、JSON-LD URL，以及 `public/robots.txt`、`public/sitemap.xml` 中的网址必须保持一致。
- 原始 HTML：`index.html` 中 `#root` 内的 `.seo-fallback` 是可直接抓取的最小页面内容。它应与 React 页面保持一致，不要在其中添加页面实际不展示的信息。
- GitHub Pages 的正确地址是 `https://mingbochen.github.io/`。

发布后，在 Google Search Console 中添加 `https://mingbochen.github.io/`，使用“网址检查”测试首页并点击“请求编入索引”，然后提交 `https://mingbochen.github.io/sitemap.xml`。搜索引擎收录通常不是即时完成的。

## 9. 本地检查与发布

第一次下载项目后安装依赖：

```bash
npm install
```

本地预览：

```bash
npm run dev
```

提交前检查：

```bash
npm run lint
npm run build
```

构建成功后会生成 `dist/`。项目已有 GitHub Pages 工作流；将确认后的改动推送到 `main` 分支后，GitHub Actions 会自动构建并发布。

## 10. 更新内容时的检查清单

1. 只填写本人确认过的真实信息。
2. 论文标题、作者顺序、会议/期刊名称与正式版本保持一致。
3. 所有外部链接都应能正常打开，不保留 `#` 占位链接。
4. 在电脑和手机宽度下各检查一次排版。
5. 运行 `npm run lint` 和 `npm run build`，确认无报错后再提交。
